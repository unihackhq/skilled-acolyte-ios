platform :ios,'10.0'

use_frameworks!

project 'Skilled Acolyte'
target 'Skilled Acolyte' do
    pod 'SVProgressHUD' # loading spinner
    pod 'JWTDecode' # jwt decoder to take collect the student id
    pod 'TPKeyboardAvoiding' # ensures content is not hidden by the keyboard
    pod 'PushNotifications' # pusher: allows subscribing to push notifications
    pod 'HockeySDK', '~> 5.1.2' # crash support when distributed via hockey app
end
target 'Skilled AcolyteTests' do
    pod 'SVProgressHUD'
    pod 'JWTDecode'
    pod 'TPKeyboardAvoiding'
    pod 'PushNotifications'
    pod 'HockeySDK', '~> 5.1.2'
end

